int meka_tri(int *mergeSize, int *triShape, int len, stack *a, stack*b)
{
    int i;
    int j;
    int size;
    int up;

    i = 0;
    while(i < len)
    {
        j = 0;
        size = mergeSize[i];
        up = triShape[i];
        while (j < size)
        {
        }
    }
}
